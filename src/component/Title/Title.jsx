import React from 'react';

const Title = ({title}) => {
 
    return (
        <div>
            <h2 className='text-4xl font-bold text-pink-500'>{title}</h2>
        </div>
    );
};

export default Title;